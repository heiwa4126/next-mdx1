// from https://www.npmjs.com/package/next-mdx-remote

import { MDXRemote } from "next-mdx-remote/rsc";

// app/page.js
export default function Home() {
  return (
    // it works only app/ dir. MDXRemote returns promise
    <MDXRemote
      source={`# Hello World

      This is from Server Components!
      `}
    />
  );
}
